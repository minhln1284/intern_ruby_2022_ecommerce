<div class="category-index-padding">
  <div class = "container">
    <div class="card">
      <div class="card-body">
        <%= render "form", product: @product %>
        <%= link_to t("admin.product.show"), admin_product_path(@product), class: "btn btn-outline-primary" %>
        <%= link_to t("admin.product.back"), admin_products_path, class: "btn btn-outline-primary" %>
      </div>
    </div>
  </div>
</div>
